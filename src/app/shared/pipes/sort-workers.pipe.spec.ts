import { SortWorkersPipe } from './sort-workers.pipe';

describe('SortWorkersPipe', () => {
  it('create an instance', () => {
    const pipe = new SortWorkersPipe();
    expect(pipe).toBeTruthy();
  });
});
