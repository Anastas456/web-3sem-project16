import { WorkerAgePipe } from './worker-age.pipe';

describe('WorkerAgePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkerAgePipe();
    expect(pipe).toBeTruthy();
  });
});
