import { WorkerDepartmentPipe } from './worker-department.pipe';

describe('WorkerDepartmentPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkerDepartmentPipe();
    expect(pipe).toBeTruthy();
  });
});
